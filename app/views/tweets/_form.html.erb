<%= form_with(model: tweet, local: true) do |form| %>
  <% if tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

      <ul>
      <% tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="control">
      <%= form.label :tweet %></br>
      <%= form.text_area :tweet, maxlength: "140", wrapper: false, class: "form-control", rows:"2", required: true, placeholder: "Write your tweet!!" %>
    </div>
  </div>
    <%= form.submit %>
<% end %>
